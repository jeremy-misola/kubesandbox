apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubesandbox-backend.fullname" . }}
  labels:
    {{- include "kubesandbox-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubesandbox-backend.selectorLabels" . | nindent 4 }}
