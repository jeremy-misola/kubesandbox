name: Build and Push ttyd Shell Image

on:
  push:
    branches:
      - main
    paths:
      - 'ttyd/**'
  pull_request:
    branches:
      - main
    paths:
      - 'ttyd/**'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Read image version
        id: version
        run: echo "version=$(cat ttyd/VERSION)" >> "$GITHUB_OUTPUT"

      - name: Login to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: jurassicjey/ttyd-k8s
          tags: |
            # Version comes from ttyd/VERSION (e.g. 1.0.0). Bump that file in
            # the same commit that pins the new tag in the Crossplane comp.
            type=raw,value=${{ steps.version.outputs.version }}
            # Rolling pointer to the newest build.
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./ttyd
          file: ./ttyd/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
